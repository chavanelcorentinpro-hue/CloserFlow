export type ProjectTask={id:string;title:string;trade:string;estimatedHours:number;status:'todo'|'doing'|'done';assigneeId?:string;stockRefs?:string[]};
export type ProjectPlan={projectId:string;title:string;startDate:string;tasks:ProjectTask[];estimatedHours:number;progress:number;warnings:string[]};
const templates:Record<string,Array<[string,string,number]>>={bathroom:[['Protection et dépose','Préparation',8],['Plomberie / évacuations','Plomberie',12],['Supports, cloisons et étanchéité','Second œuvre',12],['Carrelage / faïence','Carrelage',20],['Sanitaires et finitions','Plomberie',12]],painting:[['Protection et préparation','Peinture',6],['Réparations / enduits','Peinture',8],['Impression','Peinture',5],['Finitions peinture','Peinture',12]],default:[['Préparation chantier','Préparation',4],['Travaux principaux','Chantier',16],['Contrôle et finitions','Finitions',4]]};
export function buildProjectPlan(input:{projectId:string;title:string;startDate?:string;category?:string}):ProjectPlan{const c=(input.category||'').toLowerCase();const key=c.includes('salle')?'bathroom':c.includes('peint')?'painting':'default';const tasks=templates[key].map(([title,trade,estimatedHours],i)=>({id:`${input.projectId}-v47-${i+1}`,title,trade,estimatedHours,status:'todo' as const}));return{projectId:input.projectId,title:input.title,startDate:input.startDate||new Date().toISOString().slice(0,10),tasks,estimatedHours:tasks.reduce((s,t)=>s+t.estimatedHours,0),progress:0,warnings:[]}}
export function analyzeProject(plan:ProjectPlan,actualHours=0,missingStock:string[]=[]){const done=plan.tasks.filter(t=>t.status==='done').length;const progress=plan.tasks.length?Math.round(done/plan.tasks.length*100):0;const warnings:string[]=[];if(actualHours>plan.estimatedHours*1.1)warnings.push('Temps passé supérieur de plus de 10 % à l’estimation.');if(missingStock.length)warnings.push(`Matériel manquant : ${missingStock.join(', ')}`);if(progress<50&&actualHours>plan.estimatedHours*.6)warnings.push('Avancement faible par rapport au temps consommé.');return{...plan,progress,warnings}}
