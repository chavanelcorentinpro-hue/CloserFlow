export type CapabilityV59='ai'|'pricing'|'cloud'|'team'|'advancedMargin';
async function api<T>(apiUrl:string,token:string,path:string,init?:RequestInit):Promise<T>{const r=await fetch(`${apiUrl.replace(/\/$/,'')}${path}`,{...init,headers:{'Content-Type':'application/json',Authorization:`Bearer ${token}`,...(init?.headers||{})}});const b=await r.json().catch(()=>({})) as any;if(!r.ok)throw new Error(b.error||`Erreur serveur (${r.status})`);return b as T;}
export async function issueCapabilityV59(apiUrl:string,token:string,capability:CapabilityV59){return api<{capability:CapabilityV59;token:string;expiresAt:string}>(apiUrl,token,'/api/capabilities/issue',{method:'POST',body:JSON.stringify({capability})});}
export async function securePricingCheckV59(apiUrl:string,token:string,input:{materialCost:number;laborHours:number;hourlyCost:number;overhead:number;targetMargin:number}){const cap=await issueCapabilityV59(apiUrl,token,'pricing');return api(apiUrl,token,'/api/protected/pricing/check',{method:'POST',headers:{'x-closerflow-capability':cap.token},body:JSON.stringify(input)});}
